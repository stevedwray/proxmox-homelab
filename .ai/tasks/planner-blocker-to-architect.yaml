# Generated by planner
planner_status: "blocked"

blocker:
  type: "missing-input"
  missing_file: ".ai/tasks/handoff-to-planner.yaml"
  reason: "Planner mode requires the handoff bundle and cannot proceed without it."

required_inputs:
  - "Create or provide .ai/tasks/handoff-to-planner.yaml"
  - "Ensure input.executor_report points to an existing report file"
  - "Ensure input.prior_architect_review points to an existing review file or null"

next_action:
  owner: "architect"
  instruction: "Regenerate planner handoff and rerun planner with that file present."
