# step-ca internal certificate authority — mgmt_seg zone
hostname: step-ca
ip_address: "10.57.1.11/24"
gateway: "10.57.1.1"
proxmox_node: pve-test
vmid: 152
cores: 1
memory: 512
swap: 256
rootfs_size: 8
rootfs_storage: infrastructure-containers
ostemplate: "storage-template:vztmpl/debian-13.1-2-docker-template.tar.gz"
tags:
  - step-ca
  - pki
  - infrastructure
depends_on:
  - apt-cacher-stack
provides:
  - service: step-ca-acme
    port: 443
    protocol: tcp

network:
  zone: mgmt_seg

ansible_playbook: "deploy-step-ca"
deployment_tier: platform
portainer_agent: false

# pve-test service IPs (explicit — do not rely on variable defaults)
portainer_server_ip: "10.57.1.20"
registry_host: "10.57.3.10"
apt_cacher_host: "10.57.3.11"
