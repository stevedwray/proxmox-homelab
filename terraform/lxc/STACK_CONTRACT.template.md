# <stack-name> — Stack Contract

<!--
INSTRUCTIONS — remove this block and every other HTML comment in this file
before saving. Nothing between `<!--` and `-->` anywhere in this template is
real contract content; none of it should survive into the filled-in file.

Copy this file to `terraform/lxc/stacks/<stack-name>/STACK_CONTRACT.md` and
replace every `<placeholder>`. Keep every section heading below exactly as
written, even when a section has nothing to say — `## Provides` and
`## Dependencies` are enforced by
`terraform/lxc/validate-stack-metadata.sh --check-contract-sections`; a
missing heading fails that check even if the content underneath would have
been correct. If a stack truly has no dependencies or provides nothing
inbound, say that explicitly under the heading (see `ci-runner-01`'s
contract for the "No inbound ports are required" phrasing) — do not delete
the heading.

For a worked, currently-passing example, read
`terraform/lxc/stacks/ci-runner-01/STACK_CONTRACT.md` alongside this
template rather than either alone.
-->

## Purpose

<One paragraph: what this stack is for and why it exists.>

## Network

| Field        | Value                    |
|--------------|--------------------------|
| Zone         | `<zone, e.g. plain LAN bridge (vmbr0) or an SDN zone name>` |
| IP           | `<ip_address value from stack.yaml, e.g. 192.168.1.60/24>` |
| Gateway      | `<gateway value from stack.yaml>` |
| VMID         | `<vmid value from stack.yaml>` |

## Inputs

| Input | Source | Notes |
|-------|--------|-------|
| <input> | <where it comes from> | <why the stack needs it> |

<!-- Omit the table rows (keep the heading) if this stack takes no inputs beyond platform defaults. -->

## Provides

| Service | Port | Protocol | Notes |
|---------|------|----------|-------|
| `<service-name>` | `<port>` | `tcp`/`udp` | <notes> |

<!-- Required heading. If nothing is exposed inbound, write "No inbound ports are required." under the heading instead of a table. -->

## Dependencies

| Stack | Why |
|-------|-----|
| `<stack-name>` | <reason> |

<!-- Required heading. If there are no dependencies, write "None." under the heading instead of a table. -->

## Persistent State

| Path | Storage | Contents |
|------|---------|----------|
| <path> | <rootfs / docker_storage / named Docker volume> | <what's stored there> |

## What May Depend On This Stack

<Who consumes this stack, or "Nothing depends on this stack" if it's a
leaf.>

## What Must Not Be Edited Casually

<Fragile, one-shot, or externally-coupled behavior a future editor should
know about before changing it — or omit this section if there is nothing
like that yet.>

## Playbook

`<ansible_playbook value from stack.yaml>`

<One sentence: which role(s)/pattern it uses — direct_stack, docker_base +
compose, systemd service, etc.>

## Implementation Files

Exact repo-relative paths for anyone (human or agent) editing this stack.
State explicitly whether each file already exists (edit in place, do not
recreate under a new path) or is new (does not exist yet, create it):

| File | Role | Status |
|------|------|--------|
| `terraform/lxc/stacks/<stack-name>/stack.yaml` | Terraform-side stack definition | <existing/new> |
| `terraform/lxc/stacks/<stack-name>/terragrunt.hcl` | Terragrunt entrypoint (boilerplate) | <existing/new> |
| `terraform/lxc/stacks/<stack-name>/inventory.yml` | Generated Terraform→Ansible handoff — do not hand-edit, regenerated by `terragrunt apply` | generated |
| `terraform/lxc/ansible/playbooks/<ansible_playbook>.yml` | Stack playbook | <existing/new> |
<!-- Add a row for terraform/lxc/ansible/roles/<role>/ only if this stack has a dedicated role — most don't. Delete this comment either way. -->
