# 01-ci-runner-02 — Register GitHub Actions runner via Ansible playbook

## Status

COMPLETE

The `deploy-ci-runner.yml` playbook was run on 2026-04-10 against `10.57.0.63`. The runner
`ci-runner-pve-test` registered successfully with labels `self-hosted`, `pve-test`, `build`,
`linux`, `x64`. The playbook generates a GitHub registration token automatically. Issue #66
closed.

## Phase

Phase 01 — CI Runner Deployment and Actions Pinning

## Prerequisites

- Task 01-01 complete: VMID 141 running at `10.57.0.63`
- `gh` CLI authenticated
- Ansible installed on workstation
- `.env` sourced with `LXC_PASSWORD`

## Objective

The runner `ci-runner-pve-test` appears as **online** in GitHub → repo → Settings → Actions → Runners with labels `self-hosted`, `pve-test`, `build`, `linux`, `x64`.

## Scope

- Run `terraform/lxc/ansible/playbooks/deploy-ci-runner.yml` against `10.57.0.63`
- Verify runner registration in GitHub API

## Out of Scope

- Workflow dispatch or CI job verification (task 01-03)
- Actions pinning (task 01-04)

## Inputs

- `terraform/lxc/ansible/playbooks/deploy-ci-runner.yml`
- GitHub token (auto-generated by playbook via `gh` CLI)
- `RUNNER_VERSION` — check https://github.com/actions/runner/releases for latest stable

## Expected Outputs

- Runner process running as systemd service inside VMID 141
- Runner listed as online in GitHub Actions settings

## Constraints and Conventions

- `no_log: true` is set on the runner configure task — token does not appear in output
- Pin `RUNNER_VERSION` to latest stable at time of deployment — never use `latest`
- The playbook mints a registration token automatically; manual token generation is optional override

## Acceptance Criteria

- [x] `systemctl status actions.runner.*` shows active/running inside LXC 141
- [x] Runner listed as online in GitHub repo Settings → Actions → Runners
- [x] Runner labels include: `self-hosted`, `pve-test`, `build`, `linux`, `x64`

## Session Prompt

```
This task is COMPLETE. ci-runner-pve-test is already registered and online.

To verify the current state:
  gh api repos/stevedwray/proxmox-homelab/actions/runners \
    --jq '.runners[] | {name, status, labels: [.labels[].name]}'
  # Expected output includes:
  # { "name": "ci-runner-pve-test", "status": "online",
  #   "labels": ["self-hosted", "pve-test", "build", "linux", "x64"] }

No action required.
```
