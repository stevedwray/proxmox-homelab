---
id: p04c-01-edge-contract
title: "Define stack-owned edge manifest contract and validation rules"
phase: "04c"
type: development
status: pending
task_doc: docs/plan/tasks/04c-stack-owned-ingress-01-edge-contract.md

context: |
  This prompt defines the contract all stack-owned ingress manifests must follow.
  Output is documentation/spec + fixtures only (no live deployment changes).

objective: |
  Produce a stable v1alpha1 contract for stack edge manifests with clear validation
  rules and examples that can be implemented independently in the next session.

preconditions:
  repository:
    - id: branch
      description: Working branch is docs/stack-owned-ingress-refactor
      verify: "git branch --show-current"
      expect: docs/stack-owned-ingress-refactor

workflow:
  steps:
    - id: 1
      name: Define schema fields and allowed values
      commands:
        - "Document required fields for apiVersion/kind/metadata/spec.routes"
        - "Define allowed auth modes and tls resolver policy"

    - id: 2
      name: Define validation rules and error catalog
      commands:
        - "Specify duplicate host detection"
        - "Specify domain suffix restriction for pve-test"
        - "Specify backend URL and auth middleware constraints"

    - id: 3
      name: Provide fixtures
      commands:
        - "Create valid examples: portainer/netbox/harbor"
        - "Create invalid examples and expected errors"

postconditions:
  criteria:
    - id: completeness
      description: Contract fully defines required fields and constraints
      verify: "Manual doc review"
      expect: No ambiguous fields remain

notes:
  - Keep this task documentation-first.
  - Do not introduce production (`pve`) targeting.
