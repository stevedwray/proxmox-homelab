# Hand-back Test Workspace (throwaway)

Tests whether `implement-step` actually writes its hand-back into this
file, not just into the chat reply. Delete this workspace once the test
is done.

## Step status

- `handbacktest-01-write-marker`: not started
