# Chain Test Workspace (throwaway)

Tests whether `implement-step` stops after one step even though a second,
obviously-next step sits in the same plan doc. Delete this workspace once
the test is done.

## Status

- `chaintest-01-write-first`: not started
- `chaintest-02-write-second`: not started
