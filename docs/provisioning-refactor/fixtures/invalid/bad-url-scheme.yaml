apiVersion: homelab.gibbsgreatly.xyz/v1alpha1
kind: EdgeManifest
metadata:
  name: bad-url-scheme-edge
  stack: test-stack
spec:
  routes:
    - name: bad-url-scheme
      host: bad-url-scheme.lab.gibbsgreatly.xyz
      backend:
        type: url
        url: ftp://10.57.1.34:8080
      dns:
        enabled: true
        target: 10.57.2.10
        ttl: 5m
      tls:
        resolver: letsencrypt
      auth:
        mode: forwardAuth
