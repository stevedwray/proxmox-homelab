# EdgeManifest v1alpha1

This file is owned by Task 04. It intentionally starts as a placeholder so
future agents can see the planned contract location before implementation work
begins.

Task 04 must replace this placeholder with the full
`homelab.gibbsgreatly.xyz/v1alpha1` `EdgeManifest` contract, including:

- required and optional fields
- backend types
- auth modes
- DNS and TLS policy
- validation expectations
- compatibility rules
- fixture and error-catalog references

Do not implement validators, renderers, or reconcilers until this contract is
complete.
