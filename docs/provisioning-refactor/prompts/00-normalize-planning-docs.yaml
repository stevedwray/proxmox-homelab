---
id: pr-00-normalize-planning-docs
title: "Normalize provisioning-refactor planning docs"
type: documentation
status: pending
task_doc: docs/provisioning-refactor/tasks/00-normalize-planning-docs.md

context: |
  This is a documentation-only task. The provisioning refactor source of truth is
  docs/provisioning-refactor/. Older Phase 04 browser-ingress docs may contain
  legacy apex-domain or central-route instructions.

objective: |
  Ensure docs clearly direct future agents to docs/provisioning-refactor for the
  stack-owned DNS, Traefik, and Authentik provisioning refactor.

workflow:
  steps:
    - id: review
      name: Review legacy docs
      commands:
        - "Search docs/prompts and docs/plan/tasks for browser ingress references"
        - "Identify conflicting hostname, DNS, or central-route instructions"
    - id: clarify
      name: Clarify source of truth
      commands:
        - "Add documentation notes only"
        - "State pve-test uses *.lab.gibbsgreatly.xyz"
        - "State CoreDNS owns lab records and MikroTik forwards"
    - id: validate
      name: Validate docs-only scope
      commands:
        - "git diff --name-only"

postconditions:
  criteria:
    - id: docs-only
      description: Only documentation files changed
    - id: no-conflict
      description: Future agents can identify provisioning-refactor as source of truth
