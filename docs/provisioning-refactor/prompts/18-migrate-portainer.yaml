---
id: pr-18-migrate-portainer
title: "Migrate Portainer route"
type: deployment
status: complete
depends_on:
  - pr-15-migrate-authentik
  - pr-17a-edge-validation-hardening
task_doc: docs/provisioning-refactor/tasks/18-migrate-portainer.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  Migrate exactly one host: portainer.lab.gibbsgreatly.xyz.

objective: |
  Move Portainer route/DNS/Auth to stack-owned provisioning with forward-auth.

workflow:
  steps:
    - Verify pve-test targeting.
    - Add portainer-stack/edge.yaml.
    - Dry-run with intended replacement host.
    - Reconcile forward-auth objects if needed.
    - Apply generated state and remove central route in one publish unit.
    - Validate UI redirect and API/agent behavior.

postconditions:
  criteria:
    - Portainer browser UI is gated and non-browser behavior is unchanged.
