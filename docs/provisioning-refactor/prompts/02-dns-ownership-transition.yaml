---
id: pr-02-dns-ownership-transition
title: "Define CoreDNS seed/generated DNS ownership transition"
type: documentation
status: complete
depends_on:
  - pr-01-bootstrap-order
task_doc: docs/provisioning-refactor/tasks/02-dns-ownership-transition.md

context: |
  CoreDNS is the lab-zone authority. MikroTik remains resolver and conditional
  forwarder.

objective: |
  Define seed/non-browser records versus manifest-generated browser records.

workflow:
  steps:
    - Update decisions and DNS renderer task/prompt docs.
    - Require generated browser records to target 10.57.2.10.
    - Describe one-host replacement during migration.

postconditions:
  criteria:
    - DNS ownership is unambiguous before renderer work begins.
