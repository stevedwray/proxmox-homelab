---
id: pr-01-bootstrap-order
title: "Document Stage 3a edge bootstrap order"
type: documentation
status: complete
depends_on:
  - pr-00-normalize-source-of-truth
task_doc: docs/provisioning-refactor/tasks/01-bootstrap-order.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  Edge manifests cannot be reconciled until the edge foundation exists.

objective: |
  Document CoreDNS seed zone -> Traefik runtime -> step-ca -> Authentik direct
  first boot/API token -> edge reconciler activation.

workflow:
  steps:
    - Read bootstrap.md and decisions.md.
    - Update only the listed files.
    - Make clear that Terraform does not run a hidden second pass.

postconditions:
  criteria:
    - Stage 3a is explicit for Mode 2 deployment.
