---
id: pr-16-migrate-harbor
title: "Migrate Harbor route"
type: deployment
status: pending
depends_on:
  - pr-15-migrate-authentik
task_doc: docs/provisioning-refactor/tasks/16-migrate-harbor.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  Migrate exactly one host: harbor.lab.gibbsgreatly.xyz.

objective: |
  Move Harbor route/DNS to stack-owned provisioning while preserving native auth.

workflow:
  steps:
    - Verify pve-test targeting.
    - Add harbor-stack/edge.yaml.
    - Dry-run with intended replacement host.
    - Apply generated state and remove central route in one publish unit.
    - Validate registry clients are not redirected to Authentik.

postconditions:
  criteria:
    - Harbor browser route works and robot/client auth remains native.
