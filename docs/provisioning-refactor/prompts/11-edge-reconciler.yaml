---
id: pr-11-edge-reconciler
title: "Implement edge reconciler"
type: development
status: complete
depends_on:
  - pr-07-traefik-renderer
  - pr-08-coredns-renderer
  - pr-10-authentik-reconciler
task_doc: docs/provisioning-refactor/tasks/11-edge-reconciler.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  The operator needs one dry-run-first command for edge reconciliation.

objective: |
  Add validate, render, plan, preflight, and optional apply orchestration.

workflow:
  steps:
    - Default to dry-run.
    - Require explicit --apply for mutation.
    - Add pve-test, CoreDNS, Traefik, and Authentik preflights.
    - Support one intended replacement host.

postconditions:
  criteria:
    - Terraform state is not mutated by the edge reconciler.
