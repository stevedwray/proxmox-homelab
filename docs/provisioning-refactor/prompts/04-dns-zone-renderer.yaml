---
id: pr-04-dns-zone-renderer
title: "Implement CoreDNS zone renderer from manifests"
type: development
status: pending
depends_on:
  - pr-02-manifest-validator
task_doc: docs/provisioning-refactor/tasks/04-dns-zone-renderer.md

context: |
  CoreDNS is the authority for lab.gibbsgreatly.xyz. MikroTik is the resolver and
  conditional forwarder. Do not manage lab browser records as MikroTik static DNS.

objective: |
  Render deterministic CoreDNS records from stack-owned edge manifests.

workflow:
  steps:
    - id: extract
      name: Extract DNS intent
      commands:
        - "Read validated manifests"
        - "Collect browser hosts and DNS targets"
    - id: render
      name: Render CoreDNS zone state
      commands:
        - "Preserve SOA, NS, and non-browser records"
        - "Render browser hosts to 10.57.2.10"
    - id: validate
      name: Validate DNS path
      commands:
        - "Validate generated zone syntax"
        - "Validate MikroTik conditional forwarding exists"

postconditions:
  criteria:
    - id: no-mikrotik-static
      description: Task does not use MikroTik static records as browser record authority
