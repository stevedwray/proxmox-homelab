---
id: pr-07-traefik-renderer
title: "Implement Traefik renderer"
type: development
status: complete
depends_on:
  - pr-05-manifest-validator
  - pr-06-legacy-route-inventory
task_doc: docs/provisioning-refactor/tasks/07-traefik-renderer.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  Render dry-run Traefik dynamic config only. Do not deploy or remove central
  routes.

objective: |
  Generate deterministic per-stack dynamic files from validated manifests.

workflow:
  steps:
    - Validate manifests first.
    - Render url and traefikService backends.
    - Enforce duplicate/collision semantics.
    - Add renderer tests.

postconditions:
  criteria:
    - Output YAML parses and is deterministic.
