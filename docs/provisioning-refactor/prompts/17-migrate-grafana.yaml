---
id: pr-17-migrate-grafana
title: "Migrate Grafana route"
type: deployment
status: pending
depends_on:
  - pr-15-migrate-authentik
task_doc: docs/provisioning-refactor/tasks/17-migrate-grafana.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  Migrate exactly one host: grafana.lab.gibbsgreatly.xyz.

objective: |
  Move Grafana route/DNS to stack-owned provisioning while preserving native OIDC.

workflow:
  steps:
    - Verify pve-test targeting.
    - Add monitoring-stack/edge.yaml.
    - Dry-run with intended replacement host.
    - Apply generated state and remove central route in one publish unit.
    - Validate no double-auth loop.

postconditions:
  criteria:
    - Grafana uses native OIDC and no Traefik forward-auth.
