---
id: pr-02-manifest-validator
title: "Implement edge manifest validator"
type: development
status: pending
depends_on:
  - pr-01-edge-manifest-contract
task_doc: docs/provisioning-refactor/tasks/02-manifest-validator.md

context: |
  Implement validation only. Do not render or deploy anything.

objective: |
  Validate all terraform/lxc/stacks/*/edge.yaml files against the v1alpha1 contract.

workflow:
  steps:
    - id: discover
      name: Discover manifests
      commands:
        - "Find terraform/lxc/stacks/*/edge.yaml"
    - id: validate
      name: Validate schema and cross-manifest rules
      commands:
        - "Validate required fields and allowed values"
        - "Reject duplicate hostnames and router names"
        - "Reject non-lab pve-test hostnames"
    - id: tests
      name: Add tests
      commands:
        - "Use valid and invalid fixtures from Task 01"

postconditions:
  criteria:
    - id: no-side-effects
      description: Validator has no deployment or runtime side effects
