---
id: pr-06-authentik-reconciler
title: "Implement Authentik reconciler"
type: development
status: pending
depends_on:
  - pr-05-authentik-discovery
task_doc: docs/provisioning-refactor/tasks/06-authentik-reconciler.md

context: |
  Build write-capable Authentik reconciliation only after read-only discovery
  exists. Deletes are out of scope.

objective: |
  Idempotently create/update Authentik objects required by stack-owned routes.

workflow:
  steps:
    - id: ownership
      name: Define object ownership
      commands:
        - "Use documented names or labels"
    - id: dry-run
      name: Add dry-run planning
      commands:
        - "Report creates and updates before apply"
    - id: apply
      name: Implement apply
      commands:
        - "Create or update supported objects only"
        - "Preserve .lab.gibbsgreatly.xyz cookie policy"
        - "Avoid deletes"
    - id: tests
      name: Prove idempotence
      commands:
        - "First apply changes"
        - "Second apply no-ops"

postconditions:
  criteria:
    - id: no-deletes
      description: Reconciler does not delete Authentik objects by default
