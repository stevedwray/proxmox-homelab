---
id: pr-21-final-cutover-cleanup
title: "Finalize stack-owned cutover"
type: deployment
status: pending
depends_on:
  - pr-16-migrate-harbor
  - pr-17-migrate-grafana
  - pr-20-migrate-traefik-dashboard
task_doc: docs/provisioning-refactor/tasks/21-final-cutover-cleanup.md

context: |
  Run only after all six browser services have been migrated one at a time.

objective: |
  Remove remaining central per-service browser route ownership and validate the
  full stack-owned model.

workflow:
  steps:
    - Verify all six edge manifests exist.
    - Run validator and edge reconciler dry-run.
    - Confirm central proxy config is shared-runtime only.
    - Validate DNS, HTTPS, cert, and auth behavior.
    - Test rollback from previous generated snapshot.

postconditions:
  criteria:
    - No central per-service browser route ownership remains.
