---
id: pr-04-edge-manifest-contract
title: "Define EdgeManifest v1alpha1 contract"
type: documentation
status: pending
depends_on:
  - pr-00-normalize-source-of-truth
  - pr-01-bootstrap-order
  - pr-02-dns-ownership-transition
  - pr-03-cutover-semantics
task_doc: docs/provisioning-refactor/tasks/04-edge-manifest-contract.md

context: |
  The validator, renderers, and reconcilers depend on this contract.

objective: |
  Add an unambiguous EdgeManifest v1alpha1 spec, fixtures, and error catalog.

workflow:
  steps:
    - Define all required and optional fields.
    - Add valid fixtures for all six browser services.
    - Add invalid fixtures for documented failure modes.

postconditions:
  criteria:
    - Another agent can implement validation without inventing fields.
