---
id: pr-05-authentik-discovery
title: "Implement read-only Authentik discovery"
type: development
status: pending
depends_on:
  - pr-02-manifest-validator
task_doc: docs/provisioning-refactor/tasks/05-authentik-discovery.md

context: |
  This task is read-only. It maps manifest auth intent to current Authentik
  providers, applications, and outposts.

objective: |
  Produce an Authentik drift report without creating, updating, or deleting objects.

workflow:
  steps:
    - id: input
      name: Read manifests and token
      commands:
        - "Validate manifests"
        - "Use ./with-secrets for Authentik API token checks"
    - id: discover
      name: Query Authentik
      commands:
        - "List applications, providers, and outposts"
        - "Do not send write methods"
    - id: report
      name: Produce drift report
      commands:
        - "Classify matching, missing, differing, and ambiguous objects"

postconditions:
  criteria:
    - id: read-only
      description: No Authentik object is modified
