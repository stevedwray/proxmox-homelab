---
id: pr-08-coredns-renderer
title: "Implement CoreDNS renderer"
type: development
status: pending
depends_on:
  - pr-05-manifest-validator
task_doc: docs/provisioning-refactor/tasks/08-coredns-renderer.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  CoreDNS is authoritative for lab.gibbsgreatly.xyz.

objective: |
  Render full lab-zone output from seed records plus generated browser records.

workflow:
  steps:
    - Preserve SOA, NS, and non-browser records.
    - Generate browser records to 10.57.2.10.
    - Add dry-run diff and tests.

postconditions:
  criteria:
    - Browser DNS is manifest-derived without MikroTik static records.
