---
id: pr-05-manifest-validator
title: "Implement side-effect-free edge manifest validator"
type: development
status: complete
depends_on:
  - pr-04-edge-manifest-contract
task_doc: docs/provisioning-refactor/tasks/05-manifest-validator.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  Validation must not render, deploy, or contact runtime services.

objective: |
  Validate terraform/lxc/stacks/*/edge.yaml against the v1alpha1 contract.

workflow:
  steps:
    - Add discovery and structured validation.
    - Add cross-manifest uniqueness checks.
    - Add fixture-based tests.

postconditions:
  criteria:
    - Validator has no runtime side effects.
