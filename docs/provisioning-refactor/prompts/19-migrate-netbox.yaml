---
id: pr-19-migrate-netbox
title: "Migrate NetBox route"
type: deployment
status: pending
depends_on:
  - pr-18-migrate-portainer
task_doc: docs/provisioning-refactor/tasks/19-migrate-netbox.md

context: |
  Migrate exactly one host: netbox.lab.gibbsgreatly.xyz.

objective: |
  Move NetBox route/DNS/Auth to stack-owned provisioning with forward-auth.

workflow:
  steps:
    - Verify pve-test targeting.
    - Add netbox-stack/edge.yaml.
    - Dry-run with intended replacement host.
    - Reconcile forward-auth objects if needed.
    - Apply generated state and remove central route in one publish unit.
    - Validate browser redirect and API credential flow.

postconditions:
  criteria:
    - NetBox browser UI is gated and API access remains usable.
