---
id: pr-09-authentik-discovery
title: "Implement read-only Authentik discovery"
type: development
status: pending
depends_on:
  - pr-05-manifest-validator
task_doc: docs/provisioning-refactor/tasks/09-authentik-discovery.md

context: |
  This task is read-only and requires Authentik API access only for live use.

objective: |
  Map manifest auth intent to current Authentik objects and produce drift
  reports.

workflow:
  steps:
    - Validate manifests.
    - Query applications, providers, and outposts.
    - Classify matching, missing, differing, unmanaged, and ambiguous objects.
    - Add mocked API tests.

postconditions:
  criteria:
    - No Authentik write methods are sent.
