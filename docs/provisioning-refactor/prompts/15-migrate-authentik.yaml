---
id: pr-15-migrate-authentik
title: "Migrate Authentik route"
type: deployment
status: pending
depends_on:
  - pr-14-shared-validation-runbook
task_doc: docs/provisioning-refactor/tasks/15-migrate-authentik.md

context: |
  Migrate exactly one host: authentik.lab.gibbsgreatly.xyz.

objective: |
  Move Authentik route/DNS to stack-owned provisioning with auth.mode none.

workflow:
  steps:
    - Verify pve-test targeting.
    - Add authentik-stack/edge.yaml.
    - Dry-run with intended replacement host.
    - Apply generated state and remove central route in one publish unit.
    - Validate no self-protection recursion.

postconditions:
  criteria:
    - Authentik routes through Traefik without forward-auth recursion.
