---
id: pr-17a-edge-validation-hardening
title: "Edge validation substrate hardening"
type: development
status: complete
depends_on:
  - pr-17-migrate-grafana
task_doc: docs/provisioning-refactor/tasks/17a-edge-validation-hardening.md
validation_note: |
  When this task changes Python, shell, or YAML files, run SonarCloud through
  ./with-secrets because the Sonar token is stored in SOPS:
  ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  Task 17 migration validation proved route success, but resolver endpoint
  10.57.2.11 was unreachable from proxy-stack, dns-stack, and pve-test.
  Also confirm generated Traefik publish path exactly matches the watched
  runtime directory to avoid manual remediation in later migrations.

objective: |
  Stabilize DNS validation contract and Traefik generated-file publish behavior
  before Task 18 forward-auth migration work.

workflow:
  steps:
    - Decide and document resolver contract used for migration proof.
    - Implement resolver and runtime preflight checks.
    - Ensure generated Traefik file publish target equals watched directory.
    - Run end-to-end validation and dry-run convergence checks.

postconditions:
  criteria:
    - Validation substrate is deterministic for Tasks 18-20.
