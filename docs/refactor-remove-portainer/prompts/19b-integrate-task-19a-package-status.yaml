---
id: rp-19b-integrate-task-19a-package-status
title: "Integrate Task 19a package status into dev/pve-test"
type: documentation
status: pending
depends_on:
  - rp-19a-integrate-destroy-helper-into-dev-pve-test
task_doc: docs/refactor-remove-portainer/tasks/19b-integrate-task-19a-package-status.md
validation_note: |
  This is a package closeout step, not a new implementation task. Preserve the
  current workspace hazards, verify the destroy-helper integration baseline is
  already present, and integrate only the narrow package registry status update
  into dev/pve-test.

context: |
  Task 19a integrated the stop-first destroy helper onto origin/dev/pve-test as
  commit 1882071, but the local package registries still need to record that
  integration as complete. This step closes only that package-state gap.

objective: |
  Integrate the narrow package-status update for Task 19a into dev/pve-test
  while preserving local workspace hazards and avoiding any rebuild-gate work.

workflow:
  steps:
    - Preserve the modified secrets file, untracked architect notes, and local package edits.
    - Verify origin/dev/pve-test already contains commit 18820711b8128c160807479e7a192a5258d88876.
    - Cut a clean short-lived branch from the refreshed dev/pve-test baseline.
    - Apply only the task-sequence.md and prompts/index.yaml status updates for Tasks 19a and 19b.
    - Validate that the isolated diff is limited to the two package registry files.
    - Run ./with-secrets /home/steve/.local/bin/sonar-scanner before merge.
    - Merge the clean package-closeout branch into dev/pve-test.
    - Write docs/refactor-remove-portainer/reports/19a-status-update-integration-report.md.

postconditions:
  criteria:
    - dev/pve-test package state marks Task 19a complete.
    - dev/pve-test package state registers Task 19b as the explicit closeout step.
    - No helper-script or runbook code changes are reopened.
    - No rebuild-gate retry occurred in this task.

stop_conditions:
  - Workspace hazards cannot be preserved non-destructively.
  - The integrated destroy-helper baseline cannot be verified.
  - The isolated diff widens beyond task-sequence.md and prompts/index.yaml.
  - Sonar reports new issues.
