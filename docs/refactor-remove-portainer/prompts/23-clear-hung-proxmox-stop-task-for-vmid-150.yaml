---
id: rp-23-clear-hung-proxmox-stop-task-for-vmid-150
title: "Clear hung Proxmox stop task for authentik-stack (vmid=150)"
type: development
status: complete
depends_on:
  - rp-22-triage-proxmox-lxc-config-lock-timeout-during-stop-first-destroy
task_doc: docs/refactor-remove-portainer/tasks/23-clear-hung-proxmox-stop-task-for-vmid-150.md
validation_note: |
  This is a host-only cleanup step. Re-check whether the live stop task still
  exists, treat self-cleared state as a no-op, and if cleanup is required
  terminate only the specific hung pct stop 150 / lxc-stop pair without
  widening into CT force-stop, lock-file removal, service restart, or rebuild
  retry.

context: |
  Task 22 showed that CT 150 remained running while an active pct stop 150 /
  lxc-stop -n 150 --kill pair was still live. The next step is to clear only
  that hung host-side stop task so the host is no longer in an in-progress stop
  state for vmid 150.

objective: |
  Clear the stuck Proxmox stop-task state for vmid 150, or confirm it already
  self-cleared as a no-op.

workflow:
  steps:
    - Verify the Task 22 report exists in the operator workspace.
    - Preserve the modified helper script, modified secrets file, .worktrees, architect notes, and local package edits.
    - Re-check pct status 150, live stop-task processes, and lock-file presence.
    - If no live stop-task processes remain, record a no-op outcome and stop.
    - If the stop-task pair is still live, terminate only those specific hung processes.
    - Verify the stop-task processes are gone and report the resulting host state.
    - Write docs/refactor-remove-portainer/reports/23-hung-stop-task-cleanup-report.md.

postconditions:
  criteria:
    - The host is no longer left with an in-progress pct stop 150 task.
    - No CT force-stop, lock-file removal, service restart, or rebuild-gate retry occurred in this task.
    - The step either closes as a no-op or as a narrow host cleanup.

stop_conditions:
  - Preflight does not confirm pve-test.
  - Task 22 evidence is missing from disk.
  - Additional live Proxmox tasks touching vmid 150 make targeted cleanup unsafe.
  - Cleanup would require stopping CT 150 directly, removing runtime lock files, or restarting Proxmox services.
  - Clearing the specific stop-task processes leaves vmid 150 in an ambiguous runtime state.
