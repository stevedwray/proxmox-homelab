---
id: rp-18-triage-proxmox-shutdown-timeout-during-destroy
title: "Triage Proxmox container shutdown timeout during rebuild-gate destroy"
type: development
status: pending
depends_on:
  - rp-17-retry-rebuild-gate-after-stale-lock-cleanup
task_doc: docs/refactor-remove-portainer/tasks/18-triage-proxmox-shutdown-timeout-during-destroy.md
validation_note: |
  This is an evidence-only triage step. Gather non-mutating host evidence about
  the shutdown timeout failure, classify the blocker, and do not rerun the full
  rebuild gate or perform manual host remediation in this task.

context: |
  Task 17 stopped at the first rebuild-gate destroy command with Proxmox
  container shutdown timeout failures while waiting for UPID tasks to complete.
  The stale storage lock issue had already been cleared by Task 16, so this is
  a new rebuild blocker that must be classified before any retry or fix work.

objective: |
  Determine whether the shutdown timeout destroy failure is transient host
  state, stale runtime state, or a repeatable behavior that needs a follow-up
  fix task.

workflow:
  steps:
    - Add Task 18 to task-sequence.md as the triage follow-up to blocked Task 17.
    - Add this prompt to prompts/index.yaml with dependency on Task 17.
    - Preserve the modified secrets file and untracked architect prompt notes.
    - Confirm the target is still pve-test.
    - Re-read the Task 17 report and map failing units to CTs where possible.
    - Gather non-mutating CT, process, UPID/task-log, and lock evidence from pve-test.
    - Classify the blocker without changing code or host state.
    - Write docs/refactor-remove-portainer/reports/18-shutdown-timeout-triage-report.md.

postconditions:
  criteria:
    - The shutdown-timeout blocker is evidenced concretely.
    - The next step is classified as retry-after-clear or new fix task.
    - No live mutation or rebuild-gate retry occurred in this task.

stop_conditions:
  - Required evidence cannot be gathered non-mutatively.
  - Host triage would require killing tasks, force-stopping CTs, or deleting runtime state.
  - Host evidence reveals a wider issue needing architecture review.
