---
id: rp-32-run-minimal-build-path-harness
title: "Run the minimal build-path harness on pve-test"
type: development
status: pending
depends_on:
  - rp-30-classify-and-prune-disposable-sdn-objects
  - rp-31-add-minimal-build-path-validation-harness
task_doc: docs/refactor-remove-portainer/tasks/32-run-minimal-build-path-harness.md
validation_note: |
  Apply only the three build-path harness stacks and validate them with the
  dedicated minimal harness. Do not widen back into the full disposable network
  matrix or the rebuild gate.

context: |
  After container strip-down and SDN classification, the package's next live
  step is a minimal build-path validation rather than another broad matrix run.

objective: |
  Prove one narrow build-path connectivity/firewall/port flow on pve-test using
  the dedicated minimal harness.

workflow:
  steps:
    - Verify Tasks 30 and 31 evidence is present and preflight confirms pve-test.
    - Apply only net-build-01, net-artifacts-01, and net-client-01.
    - Run the minimal build-path validation harness.
    - Leave the harness containers available for the next SDN idempotency task if validation passes.
    - Write docs/refactor-remove-portainer/reports/32-run-minimal-build-path-harness-report.md.
    - Stop after reporting and return control to the architect session.

postconditions:
  criteria:
    - The minimal build-path harness is live and validated, or the first blocker is reported.
    - The task does not reintroduce the broad nine-container matrix.
    - No Sonar or Snyk scan is required for this exploratory validation step.

stop_conditions:
  - Preflight does not confirm pve-test.
  - Task 30 or Task 31 evidence is missing from disk.
  - Any minimal-harness stack apply fails.
  - The minimal harness reports an unexpected connectivity result.
