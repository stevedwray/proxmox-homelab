---
id: rp-15a-integrate-task-15-status-sync
title: "Integrate Task 15 package status into dev/pve-test"
type: documentation
status: pending
depends_on:
  - rp-15-triage-storage-lock-contention
task_doc: docs/refactor-remove-portainer/tasks/15a-integrate-task-15-status-sync.md
validation_note: |
  This is an integration-only step, not a new implementation task. Preserve the
  current workspace hazards, isolate the narrow Task 15 status-sync commit, run
  Sonar before merge, and integrate only that change into dev/pve-test.

context: |
  Task 15 evidence and package status were completed locally, and commit
  de717554a3f91a9261bd6b40e7586d4405144d4e records the intended package-status
  update. The original chore/task-15-status-sync branch is no longer safe to
  merge wholesale because it now also contains unrelated router commits.

objective: |
  Integrate only the validated Task 15 package-status commit into dev/pve-test
  while preserving local workspace hazards and avoiding any rebuild-gate or
  host-cleanup work.

workflow:
  steps:
    - Add Task 15a to task-sequence.md as the explicit integration-closeout step.
    - Add this prompt to prompts/index.yaml with dependency on Task 15.
    - Preserve the modified secrets file and untracked architect prompt notes.
    - Cut a clean short-lived branch from dev/pve-test.
    - Cherry-pick only de717554a3f91a9261bd6b40e7586d4405144d4e onto that branch.
    - Validate that the isolated diff is limited to task-sequence.md and prompts/index.yaml.
    - Run ./with-secrets /home/steve/.local/bin/sonar-scanner before merge.
    - Merge the clean integration branch into dev/pve-test.
    - Write docs/refactor-remove-portainer/reports/15-status-update-integration-report.md.

postconditions:
  criteria:
    - dev/pve-test contains the Task 15 package-status update.
    - No unrelated router changes are integrated with the status-sync commit.
    - Local workspace hazards remain preserved.
    - No rebuild-gate retry or host lock cleanup occurred in this task.

stop_conditions:
  - Workspace hazards cannot be preserved non-destructively.
  - Commit de717554a3f91a9261bd6b40e7586d4405144d4e no longer applies cleanly.
  - The isolated integration diff widens beyond the two package status files.
  - Sonar reports new issues.
