---
id: rp-29a-manually-remove-orphaned-disposable-validation-containers
title: "Manually remove orphaned disposable validation containers on pve-test"
type: development
status: pending
depends_on:
  - rp-29-strip-down-disposable-validation-containers
task_doc: docs/refactor-remove-portainer/tasks/29a-manually-remove-orphaned-disposable-validation-containers.md
validation_note: |
  This is a narrow cleanup unblocker. Remove only the orphaned disposable
  validation CTs directly on the Proxmox host, capture before/after host
  evidence, and stop on the first stop/destroy failure or retained-stack
  surprise. Do not touch SDN objects in this task.

context: |
  The corrected Task 29 rerun proved that the disposable validation CTs still
  exist on pve-test while the corresponding Terraform state is empty. That
  means the CTs are orphaned host objects and need a narrow manual cleanup task
  before SDN classification or minimal-harness work can continue.

objective: |
  Remove the orphaned disposable validation containers from pve-test and
  re-establish the reduced host baseline required for Task 30 and Task 31.

workflow:
  steps:
    - Verify Task 29 evidence is present and explicitly shows the orphaned CT blocker.
    - Verify preflight confirms pve-test.
    - Capture the current pct list before live mutation.
    - Stop and destroy only the in-scope orphaned disposable VMIDs.
    - Record already-absent VMIDs as explicit no-ops.
    - Stop immediately if any in-scope stop/destroy fails or a retained CT is unexpectedly affected.
    - Capture the pct list after the cleanup pass.
    - Write docs/refactor-remove-portainer/reports/29a-manual-orphaned-container-cleanup-report.md.
    - Stop after reporting and return control to the architect session.

postconditions:
  criteria:
    - Orphaned disposable validation CTs are absent or the first blocker is reported clearly.
    - No SDN object pruning is attempted here.
    - No Sonar or Snyk scan is required for this exploratory cleanup unblocker.

stop_conditions:
  - Preflight does not confirm pve-test.
  - Task 29 evidence is missing from disk or does not show the orphaned CT blocker.
  - Any in-scope orphaned CT stop or destroy fails.
  - A retained stack outside scope is touched or affected.
