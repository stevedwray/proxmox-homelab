---
id: rp-08b-retire-legacy-stack-cleanup-path
title: "Retire legacy stack_cleanup Ansible path before inventory-handoff validation"
type: development
status: pending
depends_on:
  - rp-08-remove-local-exec
task_doc: docs/refactor-remove-portainer/tasks/08b-retire-legacy-stack-cleanup-ansible-path.md
validation_note: |
  Run source-only checks proving the destroy-time cleanup playbook invocation is
  removed from terraform/lxc/main.tf before any Task 08a retry.
  Run SonarCloud: ./with-secrets /home/steve/.local/bin/sonar-scanner

context: |
  Task 08a attempted to generate a real harbor-stack inventory handoff artifact
  but hit a stop condition: destroy-time null_resource.stack_cleanup behavior
  invoked ansible-playbook cleanup.yml during terragrunt apply. That hidden
  path makes 08a unsafe to retry unchanged.

  This task is the narrow predecessor that retires that legacy cleanup path so
  08a can proceed with source-first validation and without hidden Ansible
  execution.

objective: |
  Apply a narrow Terraform remediation that removes or neutralizes legacy
  destroy-time stack_cleanup Ansible cleanup behavior in terraform/lxc/main.tf,
  then validate that 08a can detect the blocker as resolved before any apply.

preconditions:
  - Task 08 is complete.
  - Runbook preflight returns pve-test.
  - Task 08a remains blocked until this task completes.

workflow:
  steps:
    - Run preflight and confirm TF_VAR_proxmox_node is exactly pve-test.
    - Confirm the legacy path exists in source using grep checks before editing.
    - Edit terraform/lxc/main.tf and retire destroy-time stack_cleanup Ansible
      cleanup behavior without widening into Task 09 or rebuild-gate work.
    - Run terraform fmt on terraform/lxc/main.tf.
    - Re-run grep checks and confirm cleanup playbook invocation is absent.
    - Run harbor-stack terragrunt plan and keep scope narrow.
    - Run ./scripts/validate-portainer-refactor-platform-plan.sh.
    - Do not run Task 08a in this session.
    - Do not start Task 09 in this session.

postconditions:
  criteria:
    - terraform/lxc/main.tf no longer contains ansible-playbook cleanup invocation
      under destroy-time stack_cleanup behavior.
    - harbor-stack source-only checks can now detect blocker resolution before apply.
    - 08a remains a separate follow-up task and is not executed in this task.
    - no hidden scope expansion into platform orchestration is introduced.

stop_conditions:
  - retirement requires bundling 08a live inventory generation in the same task.
  - retirement requires implementing provision.sh or other Task 09 behavior.
  - a narrow Terraform-code-change remediation cannot be completed without
    reopening architecture decisions.
  - validation reveals unexpected tracked file changes outside task scope.

rollback_guidance: |
  If validation fails after edits, revert only Task 08b-scoped Terraform
  changes on the branch, do not retry 08a in the same session, and report the
  exact failing command/output as a stop condition.
