---
id: rp-00a-plan-baseline
title: "Establish scoped Terragrunt validation baseline"
type: development
status: complete
depends_on: []
task_doc: docs/refactor-remove-portainer/tasks/00a-establish-scoped-terragrunt-validation-baseline.md
validation_note: |
  Run the pve-test preflight, shellcheck the new helper script, then run the
  helper script itself to confirm the refactor-scoped dry-plan path works
  without root-unit, net-*, or test-docker/test-lxc null-handling failures.
branching_note: |
  Cut a short-lived branch from dev/pve-test for this one task. Validate on
  that branch. Do not merge to main. If the task is verified and tied to an
  issue, commit with Closes #N and report the branch name, commit SHA, and
  merge readiness back to the architecture session.
