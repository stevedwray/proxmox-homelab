---
id: rp-30a-validate-ci-runner-container-creation
title: "Validate retained container creation with ci-runner-01"
type: development
status: complete
depends_on:
  - rp-30-classify-and-prune-disposable-sdn-objects
task_doc: docs/refactor-remove-portainer/tasks/30a-validate-ci-runner-container-creation.md
validation_note: |
  This is a narrow live creation test. Apply only ci-runner-01, verify that
  the LXC is actually created on pve-test, and stop on the first apply or
  creation-validation failure. Do not widen into a multi-stack run.

context: |
  After orphaned disposable CT cleanup and the validated no-op SDN
  classification step, the next architecture-approved live test is a proper
  retained-stack container creation check for ci-runner-01.

objective: |
  Prove that ci-runner-01 can be created cleanly on pve-test as the first
  retained build-segment creation test after cleanup.

workflow:
  steps:
    - Verify Task 30 evidence is present, explicitly marked complete, and preflight confirms pve-test.
    - Capture the current pct list before live mutation.
    - Apply only terraform/lxc/stacks/ci-runner-01.
    - Verify that VMID 141 exists after apply with the expected target stack identity.
    - Capture pct list after apply.
    - Write docs/refactor-remove-portainer/reports/30a-validate-ci-runner-container-creation-report.md.
    - Stop after reporting and return control to the architect session.

postconditions:
  criteria:
    - ci-runner-01 is created or the first blocker is reported clearly.
    - The task remains scoped to a single retained-stack creation test.
    - No Sonar or Snyk scan is required for this exploratory live creation step.

stop_conditions:
  - Preflight does not confirm pve-test.
  - Task 30 evidence is missing from disk or does not show Status: complete.
  - ci-runner-01 apply fails.
  - VMID 141 is not created as expected after apply.
  - A retained non-target CT is unexpectedly affected.
