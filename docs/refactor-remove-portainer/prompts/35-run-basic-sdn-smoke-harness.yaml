---
id: rp-35-run-basic-sdn-smoke-harness
title: "Run the basic SDN smoke harness on pve-test"
type: development
status: pending
depends_on:
  - rp-34-add-basic-sdn-smoke-harness
task_doc: docs/refactor-remove-portainer/tasks/35-run-basic-sdn-smoke-harness.md
validation_note: |
  Apply and validate only test-lxc plus net-build-01 using the basic harness.

context: |
  This task proves we can execute a coded SDN validation path with fewer moving
  parts before returning to broader package tasks.

objective: |
  Run the basic harness on pve-test and capture concrete evidence of behavior.

workflow:
  steps:
    - Preflight TF_VAR_proxmox_node and Task 34 evidence.
    - Run validate-sdn-basic.sh apply.
    - Run validate-sdn-basic.sh validate.
    - Capture pct list evidence and write report.

postconditions:
  criteria:
    - Basic harness deploy/validate executed in live environment.
    - First blocker (if any) isolated with report evidence.

stop_conditions:
  - Preflight does not confirm pve-test.
  - Stack apply fails.
  - Harness validation cannot complete.
