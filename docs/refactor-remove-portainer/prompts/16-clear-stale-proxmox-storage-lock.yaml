---
id: rp-16-clear-stale-proxmox-storage-lock
title: "Clear or confirm absence of stale Proxmox storage lock"
type: development
status: pending
depends_on:
  - rp-15a-integrate-task-15-status-sync
task_doc: docs/refactor-remove-portainer/tasks/16-clear-stale-proxmox-storage-lock.md
validation_note: |
  This is a host-only cleanup step, not a rebuild-gate retry. Re-check whether
  the lock is present, stop if it appears active, allow a no-op if it is
  already absent, and otherwise remove only the single stale lock file.

context: |
  Task 15 classified the infrastructure-containers lock as stale/inactive host
  state, and Task 15a integrated that package status on dev/pve-test. The next
  operational step is to clear the stale lock, or confirm it is already gone,
  before a fresh rebuild-gate retry is scheduled.

objective: |
  Ensure /var/lock/pve-manager/pve-storage-infrastructure-containers is absent
  without widening into rebuild execution or unrelated host remediation.

workflow:
  steps:
    - Add Task 16 to task-sequence.md as the host-cleanup step after Task 15a.
    - Add this prompt to prompts/index.yaml with dependency on Task 15a.
    - Preserve the modified secrets file and untracked architect prompt notes.
    - Confirm the target is still pve-test.
    - Gather fresh lock, process, task-log, and storage evidence from pve-test.
    - If the lock is already absent, report a no-op and stop.
    - If the lock appears active, stop and report blocked.
    - Otherwise remove only /var/lock/pve-manager/pve-storage-infrastructure-containers.
    - Verify the lock is absent and write docs/refactor-remove-portainer/reports/16-stale-lock-cleanup-report.md.

postconditions:
  criteria:
    - The scoped lock file is absent after the task, or the task reports a no-op because it was already absent.
    - No rebuild-gate retry occurred in this task.
    - No Terraform, Ansible, or runbook files were changed.

stop_conditions:
  - Preflight does not confirm pve-test.
  - Evidence suggests the lock is actively held.
  - Cleanup would require deleting anything other than the single scoped lock file.
  - Host evidence reveals a wider Proxmox/runtime defect.
