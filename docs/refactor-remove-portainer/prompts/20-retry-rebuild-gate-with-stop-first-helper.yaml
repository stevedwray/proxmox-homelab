---
id: rp-20-retry-rebuild-gate-with-stop-first-helper
title: "Retry the full pve-test rebuild gate with the stop-first destroy helper"
type: development
status: pending
depends_on:
  - rp-19b-integrate-task-19a-package-status
task_doc: docs/refactor-remove-portainer/tasks/20-retry-rebuild-gate-with-stop-first-helper.md
validation_note: |
  This is an integration validation step, not a new implementation task. Use
  the stop-first destroy helper from the runbook, stop on the first failure,
  and report the exact rebuild-gate outcome without widening into fix work.

context: |
  Task 17 failed during destroy because Proxmox container shutdown timed out.
  Task 18 classified the issue, Task 19 introduced the stop-first destroy
  helper, and Tasks 19a/19b integrated both the implementation and package
  closeout into dev/pve-test.

objective: |
  Retry the documented full rebuild gate on pve-test using the stop-first
  destroy helper and record whether the refactor now passes the final gate.

workflow:
  steps:
    - Preserve the modified secrets file, untracked architect notes, and local package edits.
    - Verify origin/dev/pve-test already contains commits 18820711b8128c160807479e7a192a5258d88876 and 7032ed57758fb4413a5c6ba0305430314ed6b288.
    - Cut a clean short-lived branch from the refreshed dev/pve-test baseline.
    - Confirm the Task 19, 19a, and 19b report files exist on disk.
    - Run the rebuild gate exactly as documented in runbook.md.
    - Stop immediately on the first failing command.
    - Write docs/refactor-remove-portainer/reports/20-rebuild-gate-with-stop-first-helper-report.md.

postconditions:
  criteria:
    - The rebuild gate either passes cleanly or fails with a precise stop condition and evidence.
    - No ad hoc fix work is mixed into this task.
    - No package or implementation files are edited in this task.

stop_conditions:
  - Preflight does not confirm pve-test.
  - The integrated baseline cannot be verified.
  - Required evidence files are missing from disk.
  - Any rebuild-gate command fails.
  - Any smoke test fails.
  - Portainer still shows platform agent endpoints.
  - The second platform provision run reports unresolved drift caused by the refactor.
