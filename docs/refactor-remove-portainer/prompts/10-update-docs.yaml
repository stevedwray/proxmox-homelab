---
id: rp-10-update-docs
title: "Create runbook and sync documentation"
type: documentation
status: pending
depends_on:
  - rp-06a-mask-remaining-tier1-playbooks
  - rp-07-classify-stacks
  - rp-08-remove-local-exec
  - rp-09-provision-script
task_doc: docs/refactor-remove-portainer/tasks/10-update-docs.md
validation_note: |
  Confirm the package now has a clear source-of-truth order and shared runbook.
  Then search repo docs for stale Portainer-everywhere / Terraform-runs-Ansible wording.

context: |
  The portainer-removal package should now follow the same method as
  docs/provisioning-refactor/: control docs, runbook, atomic tasks, and clearly
  marked background documents. Repo-level docs also need to reflect the target
  two-phase model and Tier 1/Tier 2 split.

objective: |
  Create the shared validation/runbook layer for this package and sync both
  package-level and repo-level documentation to the target model.

workflow:
  steps:
    - Read the package control docs and the repo-level docs in full.
    - Create or update runbook.md so this package has shared validation, rebuild,
      and rollback vocabulary.
    - Ensure README.md, decisions.md, and task-sequence.md clearly describe
      source-of-truth ordering, one-task-per-session execution, and background-vs-operational docs.
    - Mark 01/02 as background context and 03 as legacy draft context.
    - Update PLATFORM_CONTRACT.md, docs/design/architecture.md, terraform/lxc/README.md,
      and docs/plan/README.md so they no longer describe the old Portainer-everywhere
      or Terraform-runs-Ansible model.

postconditions:
  criteria:
    - The package has a shared runbook and clear source-of-truth order.
    - Background docs are explicitly marked as background or legacy.
    - No documentation states Terraform invokes Ansible for stack configuration.
    - ADR-04 reflects Tier 1 has no Portainer agent.
    - PLATFORM_CONTRACT.md lists direct_stack and deployment_tier.

stop_conditions:
  - A repo-level document still needs implementation decisions that belong in a
    different task — report the boundary rather than widening scope.
  - A background document must remain historically contradictory — add a status
    note rather than silently rewriting history.
