---
id: rp-34-add-basic-sdn-smoke-harness
title: "Add a basic SDN smoke harness with simple container scope"
type: development
status: pending
depends_on:
  - rp-30g-validate-external-build-trunk-path
task_doc: docs/refactor-remove-portainer/tasks/34-add-basic-sdn-smoke-harness.md
validation_note: |
  Add a low-blast-radius harness for test-lxc plus net-build-01 only.
  Validate with shellcheck and ansible-lint.

context: |
  The previous SDN recovery path was too broad. The package now adds a smaller
  harness so deployment and validation behavior can be proven in a narrow scope.

objective: |
  Add a runnable basic SDN smoke harness that keeps container count and use case
  complexity intentionally small.

workflow:
  steps:
    - Verify Task 30g report is present and marked complete.
    - Add terraform/lxc/validate-sdn-basic.sh for apply/validate/destroy/cycle.
    - Add terraform/lxc/ansible/playbooks/validate-sdn-basic.yml.
    - Lint with shellcheck and ansible-lint.
    - Write docs/refactor-remove-portainer/reports/34-add-basic-sdn-smoke-harness-report.md.

postconditions:
  criteria:
    - Harness scope is test-lxc plus net-build-01 only.
    - No live mutation is required for this coding task.

stop_conditions:
  - Task 30g report missing or not marked complete.
  - Harness scope widens beyond two stacks.
  - shellcheck or ansible-lint fails and cannot be resolved in scope.
