---
id: rp-17-retry-rebuild-gate-after-stale-lock-cleanup
title: "Retry the full pve-test rebuild gate after stale-lock cleanup"
type: development
status: pending
depends_on:
  - rp-16-clear-stale-proxmox-storage-lock
task_doc: docs/refactor-remove-portainer/tasks/17-retry-rebuild-gate-after-stale-lock-cleanup.md
validation_note: |
  This is a fresh rebuild-gate retry, not a fix task. Execute the runbook flow
  exactly, stop on the first failure or runbook stop condition, and record the
  exact failing step if the gate does not complete.

context: |
  Earlier rebuild-gate retries were blocked first by package/runbook command
  issues and then by stale host lock state on
  /var/lock/pve-manager/pve-storage-infrastructure-containers. Task 16 cleared
  or confirmed the absence of that stale lock. The next operational step is a
  fresh rebuild-gate retry under the current runbook contract.

objective: |
  Prove the refactor end to end by executing the documented two-phase pve-test
  rebuild gate, or capture the exact failing step for the next architecture
  iteration if the gate still stops.

workflow:
  steps:
    - Add Task 17 to task-sequence.md as the rebuild-gate retry step after Task 16.
    - Add this prompt to prompts/index.yaml with dependency on Task 16.
    - Preserve the modified secrets file and untracked architect prompt notes.
    - Confirm the target is still pve-test.
    - Confirm Task 16 evidence is present and Task 15a remains integrated on dev/pve-test.
    - Run destroy/apply from terraform/lxc/stacks using the runbook commands.
    - Run platform provisioning, smoke tests, Portainer endpoint verification, and the no-op rerun.
    - Stop immediately on the first failure or stop condition.
    - Write docs/refactor-remove-portainer/reports/17-rebuild-gate-after-lock-cleanup-report.md.

postconditions:
  criteria:
    - The full rebuild gate passes, or
    - the exact failing step is captured for the next architecture iteration.
    - No fix work is performed in this task.

stop_conditions:
  - Preflight does not confirm pve-test.
  - Task 16 evidence is missing from disk.
  - Any rebuild-gate command fails.
  - Any smoke test fails.
  - Portainer still shows platform endpoints.
  - The second provision run reports unresolved drift.
