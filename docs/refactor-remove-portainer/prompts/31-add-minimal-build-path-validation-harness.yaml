---
id: rp-31-add-minimal-build-path-validation-harness
title: "Add a minimal build-path validation harness"
type: development
status: pending
depends_on:
  - rp-30e-reconcile-build-seg-vlan-data-plane
task_doc: docs/refactor-remove-portainer/tasks/31-add-minimal-build-path-validation-harness.md
validation_note: |
  This is a tooling task. Add a dedicated minimal harness for net-build-01,
  net-artifacts-01, and net-client-01, and validate it with shellcheck and
  ansible-lint only. Do not run the live harness on pve-test in this task.

context: |
  The full nine-container validator is too broad for the current cleanup-first
  recovery mode. The package needs a dedicated minimal harness before more live
  SDN validation resumes.

objective: |
  Add a minimal build-path validation harness that can be run independently of
  the broad disposable network matrix.

workflow:
  steps:
    - Verify Task 30e evidence is present and explicitly marked complete.
    - Add the minimal harness script and playbook for the three-stack build path.
    - Keep the harness scoped to those three stacks only.
    - Validate the new harness with shellcheck and ansible-lint.
    - Write docs/refactor-remove-portainer/reports/31-add-minimal-build-path-validation-harness-report.md.
    - Stop after reporting and return control to the architect session.

postconditions:
  criteria:
    - A dedicated minimal harness exists for the build path.
    - The task is source-only and performs no live pve-test mutation.
    - No Sonar or Snyk scan is required for this exploratory tooling step.

stop_conditions:
  - Task 30e evidence is missing from disk or does not show Status: complete.
  - The harness cannot be kept scoped to the three named stacks.
  - shellcheck or ansible-lint fails and cannot be resolved within scope.
