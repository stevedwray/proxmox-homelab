# Teardown/Rebuild Rehearsal Closeout (20260422-044416)

## Execution Summary

- Branch: docs/teardown-test-execution-variables
- Final commit at closeout: 23dea28 (`fix(sdn): guard subnet delete behind safe vnet gate`)
- Evidence stamp: 20260422-044416
- Evidence path: docs/teardown-test/evidence/20260422-044416/logs/

## Phase Results

- Destroy phase (OP-07..OP-16): Passed
  - Full teardown batch completed for in-scope rehearsal stacks.
- Rebuild phase (OP-17..OP-27): Passed
  - Rebuilt and validated: VMIDs 120, 121, 141, 142, 143, 150, 151, 152, 153, 154.
- Edge activation (OP-25): Passed
  - Generated CoreDNS/Traefik artifacts published.
  - Reconciler apply passed when run with direct Authentik URL override.
- Final validation (OP-28): Passed
  - VMID/IP matrix, SDN topology, DNS/routing/auth behavior, direct service health, and final reconciler dry-run all passed.

## Deviations And Follow-ups

1. OP-07/OP-08 pre-fix SDN subnet side effect
   - Observed early-cycle side effect where shared SDN subnet resources could be removed while still needed by later operations.
   - Mitigation now in place via SDN destroy-hook guard logic.

2. SDN destroy-hook fix integration
   - Fix was implemented and is present on execution branch.
   - Verified at closeout by HEAD commit `23dea28`.

3. OP-25 reconciler default Authentik URL
   - Default reconciler target produced Authentik discovery HTTP 404.
   - Workaround succeeded: `--authentik-url http://10.57.1.10:9000`.
   - Follow-up: make direct Authentik URL the safe default for rehearsal/runbook workflows or document explicit required flag in all validation commands.

4. OP-28 Portainer direct probe correction
   - Initial validation probe used incorrect endpoint/port for Portainer API.
   - Corrected to `http://10.57.1.20:9000/api/system/status`; validation then passed.
   - Follow-up: update runbook validation snippet to pin Portainer API checks to port 9000.

5. Root certificate drift after step-ca rebuild
   - `certs/homelab-root.crt` changed during rebuild and remains unstaged.
   - Explicit decision still required to track new cert, ignore, or restore previous file.

## Closeout State

- OP-29 closeout documentation completed.
- Runtime evidence remains under the stamped evidence directory.
- No evidence artifacts staged in git by default.
- `certs/homelab-root.crt` remains unstaged pending operator decision.
