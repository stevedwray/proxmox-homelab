---
id: tt-04-source-edge-preflight
title: "Run source and edge preflight"
type: validation
status: pending
depends_on:
  - tt-03-backup-restore-readiness
task_doc: docs/teardown-test/tasks/04-source-edge-preflight.md

context: |
  Before destruction, prove the selected source commit can render and reconcile
  the edge model from scratch.

objective: |
  Validate source, refresh generated artifacts, and run dry-runs.

workflow:
  steps:
    - Confirm clean working tree and pve-test target guard.
    - Run manifest validation and edge unit tests.
    - Remove stale ignored generated edge artifacts.
    - Regenerate Traefik and CoreDNS artifacts.
    - Run full edge reconciler dry-run.
    - Stop on any failed validation.

postconditions:
  criteria:
    - Fresh generated artifacts and preflight evidence exist.
