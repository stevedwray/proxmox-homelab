---
id: tt-07-edge-foundation-redeploy
title: "Redeploy edge foundation"
type: deployment
status: pending
depends_on:
  - tt-06-foundation-redeploy
task_doc: docs/teardown-test/tasks/07-edge-foundation-redeploy.md

context: |
  Stage 3a solves the edge bootstrap problem before generated browser routes are
  applied.

objective: |
  Redeploy CoreDNS, Traefik, step-ca, and Authentik direct/API bootstrap.

workflow:
  steps:
    - Deploy Stage 3a in the approved order.
    - Validate CoreDNS authority and delegated forwarding.
    - Validate Traefik HTTPS reachability.
    - Validate step-ca readiness.
    - Complete or verify Authentik direct first boot and API token.
    - Stop if generated edge state is required too early.

postconditions:
  criteria:
    - Edge reconciler prerequisites are healthy.
