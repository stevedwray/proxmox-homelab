---
id: tt-00-normalize-completion-state
title: "Normalize completion state before teardown test"
type: documentation
status: pending
task_doc: docs/teardown-test/tasks/00-normalize-completion-state.md

context: |
  The stack-owned edge refactor appears implemented, but task bookkeeping and
  ignored generated artifacts may mislead a destructive rebuild test.

objective: |
  Reconcile completion metadata and record the source commit selected for the
  teardown/deploy rehearsal.

workflow:
  steps:
    - Read the task doc, teardown-test README, and provisioning-refactor index.
    - Update only documentation/status files.
    - Do not run destroy, apply, or deployment commands.
    - Document that .generated artifacts must be regenerated before publish.

postconditions:
  criteria:
    - Future agents can see the rebuild test source state clearly.
