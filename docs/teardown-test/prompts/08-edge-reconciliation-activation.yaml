---
id: tt-08-edge-reconciliation-activation
title: "Activate edge reconciliation"
type: deployment
status: pending
depends_on:
  - tt-07-edge-foundation-redeploy
task_doc: docs/teardown-test/tasks/08-edge-reconciliation-activation.md

context: |
  After Stage 3a, generated DNS, Traefik, and Authentik state can become active.

objective: |
  Publish generated edge state from fresh manifests and reconcile Authentik.

workflow:
  steps:
    - Regenerate CoreDNS and Traefik artifacts.
    - Run edge reconciler apply with explicit operator intent.
    - Publish generated CoreDNS zone.
    - Publish generated Traefik files.
    - Validate all six browser routes.
    - Run full baseline dry-run after publish.

postconditions:
  criteria:
    - Stack-owned edge state is active and converged.
