---
id: tt-05-destroy-rehearsal
title: "Execute approved destroy rehearsal"
type: destructive-execution
status: pending
depends_on:
  - tt-04-source-edge-preflight
task_doc: docs/teardown-test/tasks/05-destroy-rehearsal.md

context: |
  This task is destructive. Run only after explicit operator approval.

objective: |
  Destroy the approved pve-test stack set and verify absence.

workflow:
  steps:
    - Reconfirm target guard returns pve-test.
    - Re-state approved stack list and rollback deadline.
    - Destroy only approved stacks in reverse order.
    - Capture evidence.
    - Verify selected VMIDs are absent.
    - Stop immediately on unexpected target or unapproved VMID.

postconditions:
  criteria:
    - Approved stack set is absent and evidence is captured.
