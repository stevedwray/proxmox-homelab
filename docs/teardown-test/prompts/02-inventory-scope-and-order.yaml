---
id: tt-02-inventory-scope-and-order
title: "Inventory scope and dependency order"
type: documentation
status: pending
depends_on:
  - tt-01-answer-test-variables
task_doc: docs/teardown-test/tasks/02-inventory-scope-and-order.md

context: |
  A pve-test wipe-and-rebuild is Mode 2 work. Bootstrap order is load-bearing.

objective: |
  Produce the exact stack scope, destroy order, deploy order, and dependency
  conflict list.

workflow:
  steps:
    - Read stack.yaml files for selected stacks.
    - Compare current dependencies with docs/design/bootstrap.md.
    - Pay special attention to Stage 3a ordering.
    - Update variables.md and runbook if the approved order changes.
    - Do not execute teardown or deploy commands.

postconditions:
  criteria:
    - The test has an approved stack order and documented exclusions.
