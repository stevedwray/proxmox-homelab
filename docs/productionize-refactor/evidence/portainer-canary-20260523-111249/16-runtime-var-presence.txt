=== PRODUCTION MUTATION APPROVED ===
Task: canary-portainer-pve-20260523
Command: bash -lc for v in PORTAINER_ADMIN_PASSWORD TF_VAR_portainer_admin_password PORTAINER_OAUTH_CLIENT_SECRET AUTHENTIK_SUPERUSER_API_TOKEN ALLOW_PVE; do if [ -n "${!v:-}" ]; then printf "PASS %s set\n" "$v"; else printf "FAIL %s missing\n" "$v"; fi; done

FAIL PORTAINER_ADMIN_PASSWORD missing
FAIL TF_VAR_portainer_admin_password missing
FAIL PORTAINER_OAUTH_CLIENT_SECRET missing
PASS AUTHENTIK_SUPERUSER_API_TOKEN set
FAIL ALLOW_PVE missing
